import { resolve } from 'path'
import { defineConfig } from 'vite'

export default defineConfig({
  build: {
    rollupOptions: {
      input: {
        main: resolve(__dirname, 'home.html'), // Set home.html as your main entry
        other: resolve(__dirname, 'destination.html'), 
      },
    },
  },
})